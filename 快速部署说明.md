# AIClient-2-API Docker 快速部署

## 📋 部署前准备

### 1. 安装 Docker Desktop

如果还没有安装 Docker，请先下载安装：

**Windows 用户**：
- 下载地址：https://www.docker.com/products/docker-desktop
- 安装后重启电脑
- 启动 Docker Desktop 并等待完全启动（托盘图标不再闪烁）

### 2. 启动 Docker Desktop

⚠️ **重要**：在运行部署脚本前，请确保：
- Docker Desktop 应用程序已启动
- 系统托盘中的 Docker 图标显示为绿色（运行中）
- 可以在命令行中运行 `docker ps` 命令

## 🚀 一键部署

### 方式一：使用自动部署脚本（最简单）

1. 确保 Docker Desktop 正在运行
2. 双击运行 `deploy.bat` 文件
3. 等待部署完成
4. 访问 http://localhost:3000

### 方式二：手动部署

打开命令提示符（CMD）或 PowerShell，执行：

```cmd
cd AIClient-2-API

# 构建镜像
docker build -t aiclient2api .

# 启动服务
docker-compose up -d

# 查看状态
docker ps
```

## 🌐 访问服务

部署成功后：

- **Web 管理界面**：http://localhost:3000
- **API 端点**：http://localhost:3000/v1/chat/completions
- **默认密码**：admin123
- **API Key**：123456

## ⚙️ 配置模型

### 使用 Gemini（推荐免费方案）

1. 获取 Google Cloud 项目 ID
2. 完成 Gemini OAuth 授权（首次使用会自动引导）
3. 编辑 `config.json`：

```json
{
    "MODEL_PROVIDER": "gemini-cli-oauth",
    "PROJECT_ID": "your-google-project-id"
}
```

4. 重启容器：`docker-compose restart`

### 使用 OpenAI

编辑 `config.json`：

```json
{
    "MODEL_PROVIDER": "openai-custom",
    "OPENAI_API_KEY": "sk-your-api-key",
    "OPENAI_BASE_URL": "https://api.openai.com/v1"
}
```

### 使用 Claude

编辑 `config.json`：

```json
{
    "MODEL_PROVIDER": "claude-custom",
    "CLAUDE_API_KEY": "sk-ant-your-api-key",
    "CLAUDE_BASE_URL": "https://api.anthropic.com"
}
```

## 🔧 常用命令

```cmd
# 查看日志
docker logs -f aiclient2api

# 重启服务
docker-compose restart

# 停止服务
docker-compose down

# 更新并重启
docker-compose up -d --build
```

## ❓ 常见问题

### Q: Docker 命令报错 "cannot find the file specified"
**A**: Docker Desktop 未运行，请先启动 Docker Desktop 应用程序

### Q: 端口 3000 被占用
**A**: 修改 `docker-compose.yml` 中的端口映射：
```yaml
ports:
  - "8080:3000"  # 改为其他端口
```

### Q: 如何修改 API Key？
**A**: 编辑 `config.json` 中的 `REQUIRED_API_KEY` 字段，然后重启容器

### Q: 如何查看详细日志？
**A**: 运行 `docker logs -f aiclient2api`

## 📚 更多信息

- 完整文档：查看 `README-DOCKER.md`
- 项目文档：https://aiproxy.justlikemaki.vip/zh/
- GitHub：https://github.com/chaorenaya/AIClient-2-API

## 🎯 下一步

1. ✅ 访问 Web UI：http://localhost:3000
2. ✅ 使用默认密码 `admin123` 登录
3. ✅ 在配置页面设置你的模型提供商
4. ✅ 测试 API 调用
5. ✅ 集成到你的应用（如 Cherry-Studio、NextChat 等）

---

**提示**：首次部署建议使用 Gemini CLI OAuth 模式，完全免费且无需 API Key！
